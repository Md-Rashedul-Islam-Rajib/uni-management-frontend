// import { ReactNode } from "react";

// type TSidebarMenu = {
//     key: string;
//     label: ReactNode;
//     children?: TSidebarMenu[];
// }
// export const sidebarMenuGenerator = (path: TSidebarMenu[]) => {
//     path.reduce(acc, item) => {
//     if(item.path && )
//     }

// // }